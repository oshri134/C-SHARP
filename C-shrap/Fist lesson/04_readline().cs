using System;

namespace lesson01
{
    class Program
    {
        static void Main(string[] args)
        {
            // Console.ReadLine() - reads a line to a variable from the console. 
            Console.WriteLine("Please enter your name:");
            string name = Console.ReadLine();
            Console.WriteLine($"Your name is: {name}");

            Console.ReadKey();

            /*
             * 
             * ask from the user to add 3 items to shopping list. 
             * insert each item to diffrent variable. 
             * print the shopping list to the console. 
             * 
             */
        }
    }
}
